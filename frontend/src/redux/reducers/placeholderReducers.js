/**
 * Placeholder Reducers
 * =====================
 * Temporary reducers to complete Redux store setup
 * Will be implemented as features are developed
 */

// dashboardReducer.js
export const dashboardReducer = (state = { stats: {}, loading: false }, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

// studentReducer.js  
export const studentReducer = (state = { students: [], loading: false }, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

// teacherReducer.js
export const teacherReducer = (state = { teachers: [], loading: false }, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

// classReducer.js
export const classReducer = (state = { classes: [], loading: false }, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

// courseReducer.js
export const courseReducer = (state = { courses: [], loading: false }, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

// gradeReducer.js
export const gradeReducer = (state = { grades: [], loading: false }, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

// attendanceReducer.js
export const attendanceReducer = (state = { records: [], loading: false }, action) => {
  switch (action.type) {
    default:
      return state;
  }
};